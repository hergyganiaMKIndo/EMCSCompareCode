using App.Data.Domain;
using App.Web.Models;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Web;

namespace App.Web.Controllers.Vetting
{
    public static class ManualVettingService
    {
        //public static IQueryable<SP_ManualVetting> FilterExt(List<SP_ManualVetting> model, ManualVettingModel filter)
        //{
        //    var query = model.AsQueryable();

        //    if (filter.OMCode != null)
        //        if (filter.OMCode.Count > 0) query = query.Where(p => filter.OMCode.Contains(p.OMCode));

        //    if (filter.HSID != null)
        //        if (filter.HSID.Count > 0) query = query.Where(p => filter.HSID.Contains(p.HSID));

        //    return query;
        //}
    }
}